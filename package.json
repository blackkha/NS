{
  "name": "nullsig-landing",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && npx tailwindcss -i ./input.css -o ./dist/output.css"
  },
  "devDependencies": {
    "tailwindcss": "^3.0.0",
    "autoprefixer": "^10.0.0",
    "postcss": "^8.0.0"
  }
}